template(name="smartEvent")
  .event
    if loaded
      h1= smartEvent.eventName
      if deleted
        .event--deleted
          h2 This event has been deleted
      else
        +eventNav smartEventId=smartEvent._id
        .event--content
          +Template.dynamic template=template.name data=template.data
    else
      +loading
